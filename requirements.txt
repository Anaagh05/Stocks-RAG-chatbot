# Core RAG Orchestration
langchain>=0.2.0
langchain-community>=0.2.0

# Vector Store
chromadb>=0.5.0

# Sparse Retrieval (BM25)
rank-bm25>=0.2.2

# Local LLM (Ollama client)
ollama>=0.2.0

# PDF Parsing - Primary (table-aware)
pdfplumber>=0.11.0

# PDF Parsing - Fallback (fast text extraction)
pymupdf>=1.24.0

# HTML Scraping
beautifulsoup4>=4.12.0
httpx>=0.27.0

# Embedding Generation + Cross-Encoder Re-ranking
# BGE model: BAAI/bge-small-en-v1.5
# Re-ranker model: cross-encoder/ms-marco-MiniLM-L-6-v2
sentence-transformers>=3.0.0

# Daily Ingestion Scheduler
apscheduler>=3.10.0

# Phase 3: API Server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0

# Phase 4: Testing
pytest>=8.0.0
pytest-mock>=3.14.0
